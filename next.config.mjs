/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    assetPrefix: '/v1ckt.github.io/',
    basePath: '/v1ckt.github.io',
    trailingSlash: true,
};

export default nextConfig;